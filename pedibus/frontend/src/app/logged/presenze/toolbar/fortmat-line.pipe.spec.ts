import { FortmatLinePipe } from './fortmat-line.pipe';

describe('FortmatLinePipe', () => {
  it('create an instance', () => {
    const pipe = new FortmatLinePipe();
    expect(pipe).toBeTruthy();
  });
});
